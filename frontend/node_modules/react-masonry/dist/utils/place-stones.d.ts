import { Spot, Stone, Position } from '../internal-types';
export declare const placeStones: ({ stones, containerSize, }: {
    stones: Stone[];
    containerSize: number;
}) => {
    availableSpots: Spot[];
    positions: Position[];
    containerHeight: number;
};
