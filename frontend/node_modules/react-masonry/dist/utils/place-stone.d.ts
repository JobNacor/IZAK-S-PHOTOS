import { Spot, Stone } from '../internal-types';
export declare function placeStone({ stone, availableSpots, containerSize, }: {
    stone: Stone;
    availableSpots: Spot[];
    containerSize: number;
}): {
    position: {
        left: any;
        top: any;
    };
    availableSpots: Spot[];
};
