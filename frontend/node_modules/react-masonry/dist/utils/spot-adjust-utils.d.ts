import { Spot, Stone, Position } from '../internal-types';
export declare const spotAdjustUtils: ({ spots, position, optimalSpot, stone, }: {
    spots: Spot[];
    position: Position;
    optimalSpot: Spot;
    stone: Stone;
}) => (Spot | null)[];
